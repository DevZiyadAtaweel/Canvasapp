import 'package:flutter/material.dart';

class AllDrawings extends StatelessWidget {
  const AllDrawings({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("All Drawings")));
  }
}
